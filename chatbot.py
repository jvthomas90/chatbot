def get_bot_response(user_response):
	if "quote" in user_response:
		return random.choise[pearlsOfWisdom,list]
	elif "produvtiv" in user_response:
		prod_topic = random.choice(gtd,xyz)
		if prod_topic == "gtd"
			print(...)
			web.open(resourceURL)
	elif "music" in user_response:
		tunes = random[classical,scatt,whatever]
		if tunes == "classical"
			print("Confutatis movement of Mozart's 'Requiem in D minor'"")
			choice = input("Wanna listen? Y/N")
				if choice.upper() == "Y"
					web.open()
				else:
					pass/break/continue?
		elif tunes == "scatt"
			web.open(scattman)
			return "gibberish lorem ipsum here"
		else:
			???
	else
		return random.choice[default,responses,here]

user_response = ""

while True:
	user_response = input(prompt)

	if user_response = "done"
		break
	else
		get_bot_Response(user_response)
		continue
